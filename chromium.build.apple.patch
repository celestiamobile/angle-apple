diff --git a/config/BUILDCONFIG.gn b/config/BUILDCONFIG.gn
index 12595c1e3..09d1ee082 100644
--- a/config/BUILDCONFIG.gn
+++ b/config/BUILDCONFIG.gn
@@ -617,7 +617,7 @@ foreach(_target_type,
 
       if (use_libcxx_modules) {
         # Add a dependency needed for Clang modules builds.
-        deps += [ "//buildtools/third_party/libc++:all_modules" ]
+        # deps += [ "//buildtools/third_party/libc++:all_modules" ]
 
         if (!defined(invoker.generate_modulemap) &&
             defined(generate_modulemap)) {
@@ -748,7 +748,7 @@ foreach(_target_type,
 
       if (use_libcxx_modules) {
         # Add a dependency needed for Clang modules builds.
-        deps += [ "//buildtools/third_party/libc++:all_modules" ]
+        # deps += [ "//buildtools/third_party/libc++:all_modules" ]
 
         if (!defined(invoker.generate_modulemap) &&
             defined(generate_modulemap)) {
diff --git a/config/compiler/BUILD.gn b/config/compiler/BUILD.gn
index e1da11405..44adf7114 100644
--- a/config/compiler/BUILD.gn
+++ b/config/compiler/BUILD.gn
@@ -577,7 +577,7 @@ config("compiler") {
     # Flags for diagnostics.
     cflags += [ "-fcolor-diagnostics" ]
     if (!is_win) {
-      cflags += [ "-fdiagnostics-show-inlining-chain" ]
+      # cflags += [ "-fdiagnostics-show-inlining-chain" ]
     } else {
       # Combine after https://github.com/llvm/llvm-project/pull/192241
       cflags += [ "/clang:-fdiagnostics-show-inlining-chain" ]
@@ -612,13 +612,6 @@ config("compiler") {
       ]
     }
 
-    # The performance improvement does not seem worth the risk. See
-    # https://crbug.com/484082200 for background and https://crrev.com/c/7593035
-    # for discussion.
-    if (!is_wasm) {
-      cflags += [ "-fno-lifetime-dse" ]
-    }
-
     # TODO(hans): Remove this once Clang generates better optimized debug info
     # by default. https://crbug.com/765793
     cflags += [
@@ -1578,7 +1571,7 @@ ubsan_hardening("c_array_bounds") {
   # the related warning about "unsafe-buffer-usage-in-static-sized-array",
   # since we know that the array bounds sanitizing will catch any out-of-
   # bounds accesses.
-  cflags = [ "-Wno-unsafe-buffer-usage-in-static-sized-array" ]
+  # cflags = [ "-Wno-unsafe-buffer-usage-in-static-sized-array" ]
 }
 
 # Enable UBSan subset to ensure we always hit `return` statement in non-void
@@ -1922,7 +1915,7 @@ config("default_warnings") {
       "-Wno-uninitialized-const-pointer",
 
       # TODO(crbug.com/495753203): Fix and re-enable.
-      "-Wno-unused-but-set-global",
+      # "-Wno-unused-but-set-global",
     ]
 
     cflags_cc += [
diff --git a/config/ios/BUILD.gn b/config/ios/BUILD.gn
index d5e80c6b3..fe2776bbc 100644
--- a/config/ios/BUILD.gn
+++ b/config/ios/BUILD.gn
@@ -322,6 +322,14 @@ group("xctest") {
 _xctrunner_path =
     "$ios_sdk_platform_path/Developer/Library/Xcode/Agents/XCTRunner.app"
 
+if (target_environment == "catalyst") {
+  _xctrunner_executable_path =
+      "$_xctrunner_path/Contents/MacOS/XCTRunner"
+} else {
+  _xctrunner_executable_path =
+      "$_xctrunner_path/XCTRunner"
+}
+
 # When building with RBE, $ios_sdk_platform_path corresponds to a symlink
 # below $root_build_dir that points to the real SDK to use. Because the files
 # are below $root_build_dir, it is not possible to list them as a target input
@@ -335,15 +343,24 @@ _xctrunner_path =
 # To workaround this, add a target that pretends to create those files
 # (but does nothing). See https://crbug.com/1061487 for why this is needed.
 if (ios_use_xcode_symlinks) {
-  action("copy_xctrunner_app") {
-    testonly = true
-    script = "//build/noop.py"
+  if (target_environment == "catalyst") {
+    outputs = [
+      "$_xctrunner_path/Contents/Info.plist",
+      "$_xctrunner_path/Contents/PkgInfo",
+      "$_xctrunner_executable_path",
+    ]
+  } else {
     outputs = [
       "$_xctrunner_path/Info.plist",
       "$_xctrunner_path/PkgInfo",
-      "$_xctrunner_path/XCTRunner",
+      "$_xctrunner_executable_path",
     ]
   }
+  action("copy_xctrunner_app") {
+    testonly = true
+    script = "//build/noop.py"
+    outputs = outputs
+  }
 }
 
 # When creating the test runner for an XCUITest, the arm64e slice of the binary
@@ -352,7 +369,7 @@ if (ios_use_xcode_symlinks) {
 action("xctest_runner_without_arm64e") {
   testonly = true
   script = "//build/config/ios/strip_arm64e.py"
-  sources = [ "$_xctrunner_path/XCTRunner" ]
+  sources = [ _xctrunner_executable_path ]
   outputs = [ "$target_out_dir/XCTRunner" ]
   args = [
     "--output",
diff --git a/config/ios/rules.gni b/config/ios/rules.gni
index 0177d5702..d25b76857 100644
--- a/config/ios/rules.gni
+++ b/config/ios/rules.gni
@@ -1401,9 +1401,13 @@ template("ios_xcuitest_test_runner_bundle") {
     testonly = true
     script = "//build/apple/plist_util.py"
 
-    sources = [
-      "$_xctrunner_path/Info.plist",
+    if (target_environment == "catalyst") {
+      sources = ["$_xctrunner_path/Contents/Info.plist"]
+    } else {
+      sources = ["$_xctrunner_path/Info.plist"]
+    }
 
+    sources += [
       # NOTE: The XCTRunnerAddition+Info.plist must come after the Info.plist
       # because it overrides the values under "CFBundleIdentifier" and
       # "CFBundleName".
@@ -1448,7 +1452,11 @@ template("ios_xcuitest_test_runner_bundle") {
     testonly = true
     visibility = [ ":$_target_name" ]
 
-    sources = [ "$_xctrunner_path/PkgInfo" ]
+    if (target_environment == "catalyst") {
+      sources = ["$_xctrunner_path/Contents/PkgInfo"]
+    } else {
+      sources = ["$_xctrunner_path/PkgInfo"]
+    }
 
     outputs = [ "{{bundle_contents_dir}}/PkgInfo" ]
 
diff --git a/config/sanitizers/sanitizers.gni b/config/sanitizers/sanitizers.gni
index 04d86c566..0ba957ed3 100644
--- a/config/sanitizers/sanitizers.gni
+++ b/config/sanitizers/sanitizers.gni
@@ -533,15 +533,15 @@ template("ubsan_hardening") {
 
     if (_condition && !is_ubsan && is_clang) {
       cflags = [
-        "-fsanitize=${invoker.sanitizer}",
-        "-fsanitize-trap=${invoker.sanitizer}",
+        # "-fsanitize=${invoker.sanitizer}",
+        # "-fsanitize-trap=${invoker.sanitizer}",
 
         # Prevents `__has_feature(undefined_behavior_sanitizer)`
         # from evaluating true. Configs defined here are intended to
         # be usable even in release builds, i.e. as widely as possible.
         # It's important not to have full-on UBSan workarounds activate
         # just because we built support for a specific sanitizer.
-        "-fsanitize-ignore-for-ubsan-feature=${invoker.sanitizer}",
+        # "-fsanitize-ignore-for-ubsan-feature=${invoker.sanitizer}",
       ]
       if (defined(invoker.cflags)) {
         cflags += invoker.cflags
diff --git a/toolchain/apple/toolchain.gni b/toolchain/apple/toolchain.gni
index a21f40edf..106562642 100644
--- a/toolchain/apple/toolchain.gni
+++ b/toolchain/apple/toolchain.gni
@@ -120,7 +120,7 @@ template("single_apple_toolchain") {
       # here to force both contexts to stay in sync.
       # LINT.IfChange(use_apple_linker)
       use_apple_linker =
-          host_os == "mac" && current_os == "mac" && !use_thin_lto &&
+          host_os == "mac" && !use_thin_lto &&
           host_cpu == "arm64" && current_cpu == "arm64" &&
           !(use_remoteexec && use_system_xcode)
       use_lld = !use_apple_linker
@@ -233,7 +233,7 @@ template("single_apple_toolchain") {
     linker_driver_args = "-Wcrl,driver,$ld"
 
     # Specify an explicit path for the strip binary.
-    _strippath = "${prefix}llvm-strip"
+    _strippath = "${prefix}strip"
     _installnametoolpath = "${prefix}llvm-install-name-tool"
     linker_driver_args += " -Wcrl,strippath,${_strippath} -Wcrl,installnametoolpath,${_installnametoolpath}"
     _enable_dsyms = enable_dsyms
